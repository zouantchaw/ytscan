[build]
builder = "DOCKERFILE"
dockerfilePath = "infrastructure/railway/media-worker/Dockerfile"
watchPatterns = [
  "package.json",
  "package-lock.json",
  "tsconfig.json",
  "tsconfig.base.json",
  "apps/api/wrangler.toml",
  "packages/core/**",
  "packages/scripts/**",
  "infrastructure/railway/media-worker/**",
]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
